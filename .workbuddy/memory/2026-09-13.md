# 2026-09-13

## 安装数模相关 skills（用户级）

用户给了 3 个 GitHub 仓库，要求安装为 skills。已全部安装到 `~/.workbuddy/skills/`，共 **30 个新 skill**（技能库总数 32 个）。

| 来源仓库 | 安装形式 | 数量 |
|---|---|---|
| yuanchen-home/cumcm-step-review | 单 skill `cumcm-step-review`（含 tools/references/assets/scripts/agents） | 1 |
| zhnnky329/MathModeling-skills | 拆成 28 个独立 skill（.claude/skills/* 平铺） | 28 |
| XiaoMaColtAI/math-modeling-skill | 单 skill `math-modeling`（重命名为 math-modeling，保留 tools/references/assets） | 1 |

- 安全审计结论：**P2 安全**。无凭据窃取、无混淆代码、无破坏性文件操作；网络调用仅指向 api.github.com / OpenAlex / drawio 图标等公开接口；hooks 仅为 echo 提示，无 postinstall 脚本。
- 安装时排除项：`.git`、`.github`、`tests`、`dsh-plugin`（面向 DeepSeek dsh 宿主，与 WorkBuddy 无关）、`imgs`。
- 已知缺项：`workflow-orchestrator` 会回退读取 `../../AGENTS.md`（仓库打包的策略文件），该文件未安装；若项目根目录没有 AGENTS.md，该 skill 的策略回退会缺失。
- 合规提醒：`cumcm-step-review/references/降重模块/` 是「去 AI 味 / AIGC 降重」模块（默认不执行），涉及规避 AIGC 检测，使用时需自行评估学校学术规范。

与本项目（26CUMCM_A，2026 国赛 A 题）高度相关，后续建模可直接调用 `cumcm-step-review` 或 `math-modeling`。
