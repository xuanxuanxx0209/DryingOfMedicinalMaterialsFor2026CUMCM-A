# 2026 年全国大学生数学建模竞赛 A 题模型合同

## 任务边界

药材视为长 0.25 m、初始半径 0.02 m 的圆柱体。题目只要求距轴线不同位置的结果，且端面总面积与侧面积之比为 0.08，因此问题一至问题三采用一维径向轴对称模型。问题四通过归一化坐标处理半径变化。温度与干基含水率分别作为热场和水分场的状态变量。

附件一提供 0 至 14400 s 的烘房温度和空气水分浓度，共 241 条数据，间隔 60 s。附件二提供 0 至 259200 s 的药材半径，共 145 条数据，间隔 1800 s。附件一观测区间内的空气边界采用分段线性插值。问题三在 14400 s 后采用 9000 至 14400 s 稳定段的观测均值，问题四按本轮合同沿用相同稳定段均值并独立检验，不能把末值外推当作权威方案。

## 问题一模型

温度场满足

\[
\rho c_p\frac{\partial T}{\partial t}
=\frac{1}{r}\frac{\partial}{\partial r}
\left(rk\frac{\partial T}{\partial r}\right).
\]

水分场满足

\[
\frac{\partial C}{\partial t}
=\frac{1}{r}\frac{\partial}{\partial r}
\left(rD(C)\frac{\partial C}{\partial r}\right).
\]

题面扩散系数的权威写法为

\[
D(C)=7\times10^{-9}\exp\left(-\frac{0.89}{C}\right)\ \mathrm{m^2/s}.
\]

Q1 文件夹中的前期整理稿曾将该式记为 \(\exp(-0.89C)\)，该写法与题面公式不一致，仅作为历史材料保留，不能进入代码、结果或论文。

初始条件为 \(T(r,0)=28\ ^\circ\mathrm C\) 与 \(C(r,0)=2.55\ \mathrm{kg/kg}\)。轴线处采用零梯度条件，表面条件为

\[
-kT_r=h(T_s-T_a),\qquad -DC_r=h_m(C_s-C_a).
\]

参数取 \(\rho=820\ \mathrm{kg/m^3}\)、\(c_p=2600\ \mathrm{J/(kg\,K)}\)、\(k=0.36\ \mathrm{W/(m\,K)}\)、\(h=25\ \mathrm{W/(m^2\,K)}\) 与 \(h_m=8\times10^{-7}\ \mathrm{m/s}\)。空间采用节点中心有限体积法，中心和表面均为半控制体，时间采用全隐式格式。水分方程用 Picard 迭代更新扩散系数。

## 问题二模型

问题二要求计算从初始时刻至 3 h 的温度场和干基含水率场，并按 0.5 h 间隔给出距轴线 0、0.5、1、1.5、2 cm 处的结果。完整结果写入 `results/result2.xlsx`，时间分辨率为 1 s，径向分辨率为 0.1 cm。附件一的 241 条有效记录覆盖 0 至 14400 s，问题二全部输出时刻均处于观测范围内，空气边界只作分段线性插值，不需要恒温延拓。

问题二沿用固定半径的一维径向热质传递模型。模型保留一个控制方程族，不另设经验对比模型。局部温度满足 Fourier 导热方程，局部含水率满足 Fick 扩散方程。附录三使热物性随含水率变化，并使水分扩散系数同时受含水率和绝对温度控制，每一时间层按局部场值更新

\[
\rho=650+128C,\quad
c_p=1450+2736\frac{C}{C+1},\quad
k=0.21+0.38\frac{C}{C+1},
\]

\[
D=2.4\times10^{-3}
\exp\left(-\frac{0.45}{C}\right)
\exp\left(-\frac{3850}{T}\right).
\]

温度方程和水分方程分别写为

\[
\rho(C)c_p(C)\frac{\partial T}{\partial t}
=\frac{1}{r}\frac{\partial}{\partial r}
\left(rk(C)\frac{\partial T}{\partial r}\right),
\]

\[
\frac{\partial C}{\partial t}
=\frac{1}{r}\frac{\partial}{\partial r}
\left(rD(C,T)\frac{\partial C}{\partial r}\right).
\]

初值取 \(T(r,0)=28\ ^\circ\mathrm C\) 与 \(C(r,0)=2.55\ \mathrm{kg/kg}\)。轴线处的温度和水分梯度均为零。问题二没有另给表面对流系数，阶段模型假定烘房设备和表面交换机制未改变，继续使用附录二的 \(h=25\ \mathrm{W/(m^2\,K)}\) 与 \(h_m=8\times10^{-7}\ \mathrm{m/s}\) 构造第三类边界。扩散系数中的温度统一使用 K，计算结果表中的温度统一转换为摄氏度。

关键假设包括药材内部服从连续介质中的径向导热和有效扩散，侧表面热质通量可由给定的对流系数描述，以及附录三的经验式适用于前三小时的局部状态。放宽任一关键假设均需改用多孔介质或含相变项的控制方程，并用实验数据重新标定参数。可放宽假设包括固定半径、忽略轴向梯度和忽略显式蒸发潜热。问题四单独处理半径变化；轴向假设可用二维模型抽样复验；潜热假设可通过加入水分损失对应的热汇后比较温度场偏差。

空间离散采用节点中心有限体积法，将 2 cm 半径划分为 160 个等长区间，得到 \(\Delta r=0.000125\ \mathrm m\)。中心和表面节点对应半控制体，内部界面系数取相邻节点局部值的算术平均。首个时间层采用后向 Euler 格式启动，后续时间层采用二阶后向差分格式。该格式保持全隐式求解并降低一阶时间截断误差，内部步长先取 \(\Delta t=1\ \mathrm s\)，若与 0.5 s 解比较后题目表格的四位小数仍不稳定，则继续减半直至满足精度要求。

每个时间层先按当前含水率更新 \(\rho\)、\(c_p\) 与 \(k\)，求解温度三对角方程组，再按新温度和当前含水率更新 \(D\)，求解水分三对角方程组。Picard 迭代使用温度最大绝对变化不超过 \(10^{-8}\ ^\circ\mathrm C\) 且含水率最大绝对变化不超过 \(10^{-10}\ \mathrm{kg/kg}\) 的双重停止条件，单步最多迭代 40 次。

数值验证同时覆盖时间步、空间步、代数残差、局部守恒和物理范围。时间加密将步长减半，空间加密将区间数增至 320。两组加密解均在题目指定的三十个时空点与权威解比较，并逐项检查四位小数是否保持不变。每次三对角求解后计算线性方程组的归一化最大残差。局部守恒通过控制体通量抵消检查，整体守恒通过域内面积加权含水率变化与表面对流通量积分的相对残差检查。温度需位于初始温度与同期烘房温度所限定的范围内，含水率不得为负，径向剖面和表面通量方向需与边界驱动力一致。

对流系数敏感性采用单因素扰动。换热系数分别取基准值的 0.8 倍和 1.2 倍，传质系数也分别取基准值的 0.8 倍和 1.2 倍，其余条件保持不变。每个情景均计算题目指定三十个时空点相对基准解的最大绝对变化，并报告 3 h 时中心值和表面值的变化。若扰动引起温度或含水率的四位小数大范围改变，论文将把沿用附录二系数列为主要不确定性来源。

模型的主要风险来自空气水分浓度与药材干基含水率的口径差异，以及有效扩散模型对孔隙迁移和蒸发潜热的合并处理。题面明确以相同的 kg/kg 单位给出两种水分浓度，阶段模型据此直接构造传质边界，但该做法只在本题口径内成立。若后续获得湿空气含湿量和药材平衡含水率之间的换算关系，应替换表面驱动力并重新计算全部结果。

Tzempelikos 等建立了圆柱物料的一维非稳态热质耦合模型，并用温度相关的 Arrhenius 型扩散系数计算内部温度与含水率分布，DOI 为 10.1016/j.jfoodeng.2015.01.017。Da Silva 等使用圆柱坐标有限体积法、全隐式格式和第三类边界处理变物性干燥，DOI 为 10.1016/j.jfoodeng.2014.05.010。Hussain 与 Dincer 对圆柱湿物体的 Fourier 导热和 Fick 扩散模型进行二维数值分析，DOI 为 10.1016/S0017-9310(03)00229-1。三项工作为本题的方程结构和离散方式提供方法依据，具体经验参数仍完全采用题面附录三。

候选图表包括附件一空气边界曲线、前三小时温度时空图、前三小时含水率时空图、物性参数随时间变化图、不同半径位置的温度曲线、不同半径位置的含水率曲线、Picard 迭代次数曲线、时间步加密误差图和空间步加密误差图。正式论文优先保留能够直接回答问题二及说明可信度的图，其余图作为支撑材料候选。

## 问题三模型

问题三要求确定固定尺寸药材的烘干时长，在论文中给出每隔 6 h 以及烘干结束时，距轴线 0、0.5、1、1.5、2 cm 处的干基含水率，并把每隔 60 s、径向每隔 0.1 cm 的完整含水率写入 `results/result3.xlsx`。输出表从 60 s 开始，不把初始状态写入题目模板。烘干时长从初始时刻计量，单位同时保留 s 与 h。

附件一的 241 条观测在 9000 至 14400 s 共形成 91 个稳定段样本。实际读取结果给出空气温度均值 50.004945 摄氏度，空气水分浓度均值 0.049993736 kg/kg。两列在稳定段内的总体标准差分别为 0.154016 摄氏度和 0.000153463 kg/kg，观测范围分别为 49.757 至 50.246 摄氏度和 0.04975 至 0.05025 kg/kg。14400 s 以前继续使用相邻观测的分段线性插值，之后把上述均值延拓为恒温恒湿边界。均值温度进入边界函数时换算为 323.154945 K。

问题三沿用问题二固定半径的一维径向变物性传热传质模型，模型族数量为一个。控制方程、初值、中心零通量条件、表面对流条件、附录三物性公式、有限体积网格和时间层内 Picard 迭代均保持不变。该模型的新增任务仅是长期边界延拓、全域最大含水率判据、首次达标事件检测和题目规定的抽样输出，不另设平均含水率模型。

定义

\[
C_{\max}(t)=\max_{0\le r\le R}C(r,t),\qquad
t_{\mathrm{dry}}=\inf\{t\ge 0:C_{\max}(t)<0.15\}.
\]

离散计算在每个时间层完成热质耦合迭代后，对全部 161 个径向节点取最大值。首次出现 \(C_{\max}^{n-1}\ge 0.15\) 且 \(C_{\max}^{n}<0.15\) 时，将 \(t_n\) 记为离散烘干结束时刻。最大值判据直接覆盖题面中的各处要求，不预设最大含水率一定出现在中心，也不允许用体积平均含水率替代。

权威计算采用半径 160 等分与 \(\Delta t=1\ \mathrm s\)，每 60 s 记录一次内部状态，阈值首次跨越若不在 60 s 记录点则追加该时刻。输出网格由内部 0.000125 m 网格线性插值到 0 至 2 cm 的 21 个规定位置。程序从零时刻连续计算至达标，最长允许推进 7 d；若 7 d 内未达标则报错，不能把上限当作烘干时长。

时间离散验证在空间网格、边界和迭代阈值不变时分别采用 1、0.5、0.25 s，0.25 s 解作为参考。1 s 解与参考解的结束时刻绝对差必须不超过 1 s，该阈值对应主计算的事件分辨率。0.5 s 解与参考解的绝对差必须不超过 0.5 s。两次加密后的结束时刻差不得增大。任一条件不满足时，权威步长回退至 0.25 s，并重新生成全部结果。

空间验证保持 1 s 时间步，把径向区间数从 160 增至 320。题定表格各单元格的最大绝对差不得超过 \(5\times10^{-5}\ \mathrm{kg/kg}\)，该阈值等于四位小数舍入半单位。两种空间网格的结束时刻相对差不得超过 0.1%。表格稳定性不通过时采用 320 区间重新生成权威结果，结束时刻相对差超限时继续加密至 640 区间复验。

每个三对角系统的归一化最大代数残差不得超过 \(10^{-12}\)，全程面积加权水分平衡的最大相对残差不得超过 \(10^{-8}\)。末时刻系统的二范数条件数高于 \(10^8\) 时触发缩放或高精度复算。双精度机器精度约为 \(2.22\times10^{-16}\)，条件数上限对应约 \(2.22\times10^{-8}\) 的误差放大量级，低于四位小数输出精度。Picard 迭代必须在 40 次以内同时达到温度 \(10^{-8}\) 摄氏度和含水率 \(10^{-10}\ \mathrm{kg/kg}\) 的停止阈值。出现非有限值、负含水率、表面水分通量与边界驱动力方向相反或最大含水率在相邻 60 s 记录间上升超过 \(10^{-8}\ \mathrm{kg/kg}\) 时直接判定失败。

长期边界假设采用单因素确定性敏感性分析。温度取稳定段均值加减 0.154016 摄氏度，空气水分浓度取稳定段均值加减 0.000153463 kg/kg，分别对应一个总体标准差扰动。另以附件末条记录 50.165 摄氏度和 0.04986 kg/kg 作为历史方案对照。每个情景均重新计算 \(t_{\mathrm{dry}}\)，并报告相对基准时长的变化率。较高空气温度应通过扩散系数缩短时长，较高空气水分浓度应通过减小表面传质驱动力延长时长；若实际趋势相反，必须检查边界符号、单位和耦合顺序。

关键假设为固定半径、一维径向轴对称传递和附录三经验式在完整干燥状态范围内仍适用。固定半径由问题三题意与问题四的尺寸变化任务划分支持；轴向简化由题目仅要求径向结果且端面总面积与侧面积之比为 0.08 支持；经验式直接来自题面。可放宽假设为 14400 s 后采用稳定段均值边界、问题二表面对流系数继续适用于恒温阶段、相邻空气观测之间线性变化以及忽略显式蒸发潜热。前三项分别用边界情景与系数扰动复验，潜热项需在取得水分蒸发焓及能量耦合口径后另行复算。

一维轴向简化必须通过端面通量上界筛查。圆柱两端总面积与侧面积之比为 \(R/L=0.08\)。保守情景把换热系数和传质系数同时乘以 1.08，相当于把端面可能提供的全部交换能力均匀叠加到侧面，且不减少任何径向驱动力。若保守情景相对基准的烘干时长变化不超过 8%，则把该变化作为忽略端面造成的模型不确定性上界并保留一维模型。8% 阈值直接取自被忽略面积占侧面积的几何上界。若时长变化超过 8%，一维简化失效，必须建立包含轴向坐标的二维圆柱模型，并用径向和轴向至少各 80 个区间复算，直至二维网格加密后的时长相对差低于 0.1%。

模型风险集中在长期边界不可观测、表面对流系数沿用和低含水率区的经验式外推。任一敏感性情景使烘干时长变化超过 5%，论文需给出区间而不能只报单点。若计算产生负含水率、径向剖面反常、表面通量方向错误、Picard 迭代不收敛或整体水分平衡误差放大，则结果无效并返回离散与边界实现检查。候选图包括稳定段空气边界、全时域径向含水率原始场、最大含水率阈值曲线、五个题定半径的长时曲线、时间步收敛、空间网格收敛、Picard 迭代、长期边界敏感性和末端径向剖面。Tzempelikos 等与 Da Silva 等关于圆柱物料变物性热质耦合和有限体积离散的论文继续作为方程与算法依据，具体参数与达标阈值只采用题面附录三和问题三原文。

## 问题四模型（2026-09-12 冻结合同）

本轮只处理 q4 的建模、求解及写作素材，不扩写论文，不重复 q1–q3 计算。Q4 原始推荐稿和题目附件只读；以下补充优先于旧 q4 程序和旧论文的解释。

| 合同项 | 定义 |
|---|---|
| 任务 | 求所有径向位置 C<0.15 的首次时刻，提供表6及 result4.xlsx |
| 数据 | 附件1：241条，0–14400 s/60 s，温度°C与空气浓度kg/kg；附件2：145条，0–259200 s/1800 s，半径cm；均无缺失、时间严格递增，半径非增，首末为2及1.198 cm |
| 结论类型 | 主模型时长、同附录4固定尺寸时长、净几何效应、结构敏感度、数值误差、完整时空场 |
| 模型 | 一个径向有效热质扩散模型族；同方程的固定尺寸反事实及形变扰动不另计机理模型 |
| 实现 | 材料坐标节点中心有限体积、BE启动后BDF2、Picard全隐式、全域阈值事件 |
| 验证 | 真实输入小例、退化对照、常场几何一致性、代数/离散平衡残差、时空加密、形变雅可比、输出域检查 |
| 风险 | 有效扩散闭合不等于完整干固体/水分质量守恒；长期边界与内部位移未实测；不能把Q3/Q4差值归因于收缩 |

题面附录4逐项核验：
\[
\rho=760+90C,\quad c_p=1850+2150C/(C+1),\quad k=0.12+0.20C/(C+1),
\quad D=4.2\times10^{-4}\exp(-0.30/C)\exp[-3850/(T_c+273.15)].
\]
密度kg/m³、比热J/(kg K)、导热系数W/(m K)、扩散系数m²/s。初始场 T_c=28°C、C=2.55 kg/kg。沿用 h=25 W/(m² K)、h_m=8×10⁻⁷ m/s，作为题面没有另给表面系数时的明确假设。空气实测期线性插值，t>14400 s 后取9000–14400 s的91点均值（约50.004945°C、0.049993736 kg/kg；代码用未舍入均值）。半径转m后线性插值，t>259200 s后保持0.01198 m。

关键假设共3项：(1) 轴对称一维径向有效扩散，忽略轴向及显式潜热，依据题目仅给径向结果和前3问统一机制；面积比不是严格误差上界，不能声称已证明端面误差。(2) 附录4是题设有效热质系数，应用于全程状态。(3) C采用随材料运动的标量有效扩散闭合，题面不足以识别局部干固体密度及水通量本构。放宽这些假设须引入新增实验/本构并重新建模。

可放宽假设：(1) 相似收缩，由a*=0.1和0.2扰动复验；(2) 后续空气稳定均值，由均值±总体标准差和末值情景复验；(3) 半径插值及末值保持，输入单调性和是否超出72h核验；(4) 传质驱动力直接采用题面的C-Ca口径，不作相对湿度换算，若有吸附等温线须重新求解。

材料坐标 ξ∈[0,1]，记 R0=0.02 m，Rend=0.01198 m：
\[
g(t)=\frac{1-R(t)/R_0}{1-R_{end}/R_0},\quad r(\xi,t)=R(t)[\xi+a_*g(t)\xi(1-\xi)],
\quad r_\xi=R(t)[1+a_*g(t)(1-2\xi)].
\]
a*=0 为主模型；0.1、0.2 仅为未校准的确定性结构扰动，不是置信区间。rξ>0保证不交叉；r(0,t)=0，r(1,t)=R(t)，g(0)=0。固定半径反事实令R=R0、g=0，全部物性/环境/初值/网格/容差与主模型相同。

统一方程为
\[
b(C)u_t|_\xi=\frac1{rr_\xi}\partial_\xi\left(\frac{r\Gamma}{r_\xi}u_\xi\right),
\]
其中热场u=T_c、b=ρcp、Γ=k；水场u=C、b=1、Γ=D。中心uξ=0；表面−Γuξ/rξ=β(u_s−u_a)，β分别为h及hm。a*=0时严格退化为Q4推荐稿的1/(ξR²)算子。这里u_t|ξ是材料导数，不再额外加入网格对流或C∇·v项。

必须保留的闭合局限：若强行将ρ/(1+C)解释为可用于守恒的局部干固体密度并同时要求固定长度、实测R(t)及上述任意内部映射，通常与干固体连续性不相容。故ρ只按题面用于热容量；不由ρ反演内部位移，不把面积平均C作为真实质量库存。主模型及敏感性均作为Q4指定有效扩散近似求解，不能宣称完整多相守恒或已识别真实内部收缩。

离散补齐：ξ_i=i/N，控制面ξ_{i+1/2}=(i+1/2)/N，端面ξ=0,1。当前时刻几何权重 W_i=(r_{i+1/2}²−r_{i-1/2}²)/2（单位m²；省略共同2πL），界面 G_{i+1/2}=r_{i+1/2}(Γ_i+Γ_{i+1})/[2(r_{i+1}−r_i)]。离散方程 b_i W_i δ_tu_i=G右(u右−u_i)−G左(u_i−u左)，表面附加−Rβ(u_N−u_a)。BE首步，随后δ_tu=(3uⁿ−4uⁿ⁻¹+uⁿ⁻²)/(2Δt)。所有历史u在同一材料坐标，权重均取当前时刻，不能对W_i u_i直接差分冒充材料导数。界面映射为二次式，节点间距/Δξ恰等于面处rξ。

Picard先用当前C更新ρcp,k求T，再用当前C及新T更新D求C；双重停止阈值1e−8°C、1e−10 kg/kg，最多40次；非收敛、非有限、负C立即报错。时间步全程1 s；计划N=320并与640比较。时间验证固定最终主网格全程0.5 s，事件差≤1 s且表6最大差≤5e−5 kg/kg；空间验证事件相对差≤0.1%、共同6h表值最大差≤5e−5（输出四位小数半单位）。失败则增大N并对最终网格重验；如跨舍入边界，明示四位显示不代表四位绝对准确。

模型离散平衡残差：水场每步 sum(W_i δ_t C_i)+R hm(C_N−Ca)=0；热场 sum(ρ_i cp_i W_i δ_tT_i)+R h(T_N−Ta)=0。水场按两侧最大绝对量归一，相对残差≤1e−8。热场稳态净通量趋零，须记录绝对残差，并以全部离散存储项绝对值之和及边界项归一（包括历史项），该归一残差≤1e−12，避免用趋零净通量相除。三对角归一残差≤1e−12、最终系统二范数条件数≤1e8。这是有效方程的离散检验，不是物理干固体总质量检验。常场与相等环境应保持常值以检验几何运动没有凭空增加浓度。a*=0应与既有均匀移动网格求解在相同dt/BDF2/均值边界下吻合。

每一步取所有节点最大C；记录上一时间层Cmax≥0.15和当前Cmax<0.15，当前层为离散终点，并保留跨越区间，禁止由60s采样决定事件。上限7 d仅为失败保护，未达标报错。温度保持初始及边界范围、C≥0，记录全程径向反向差及中心距最大值，不预设中心控制。

result4.xlsx从60 s开始每60 s保存，追加非整分钟终点；固定实际位置0,0.1,…,1.9 cm按实际r节点插值，r>R(t)处留空（等于R时允许表面值），独立末列始终为药材表面，全部有效水分值四位小数格式。表6取6h倍数及终点，固定0,0.5,1,1.5 cm加独立表面列；另附R(t)和结构性空值说明。域外留空不是数据缺失，禁止将被收缩掉的固定位置填成表面值。高精度npz/CSV用于阈值及数值验证，Excel保留未舍入浮点并采用0.0000显示。

指标：Δt_R=t4−t40，η_R=100(t4−t40)/t40；η_a=100[t(a)−t(0)]/t(0)，E_shape=max(|η_0.1|,|η_0.2|)。结构偏差是否小由实际结果解释，不预写稳健结论；±空气标准差是情景扰动而非统计置信区间。

候选图q4范围至少9张：raw半径实测/插值、稳定空气分布、收缩率对半径关系；process映射/Jacobian、迭代分布、时空加密差；result阈值曲线、移动域含水率场、几何反事实对照、结构敏感性及题定径向剖面。每图保存源数据、SVG/PDF/300DPI PNG和灰度图。

文献核验：双引擎hybrid_scholar.py于2026-09-12运行，网络限制重试后成功，结果存build/q4_audit/literature_search.json。方法背景可追溯Adrover, Brasiello, Ponso (2019), A moving boundary model for food isothermal drying and shrinkage: General setting, Journal of Food Engineering 244:178–191，DOI 10.1016/j.jfoodeng.2018.09.018，作者机构原始记录 https://iris.uniroma1.it/handle/11573/1156871 。该文使用额外局部收缩闭合，只支持移动边界需要闭合的背景，不能据此称Q4具体映射已被实验验证。另参见同作者2019年一维/二维比较 DOI 10.1111/jfpe.13178，https://onlinelibrary.wiley.com/doi/10.1111/jfpe.13178 ，面积比不能替代二维误差验证。

阶段范围及规则：官方2026竞赛时间9月10日18:00至9月13日20:00（北京时间），来源https://en.mcm.edu.cn/及https://www.csiam.org.cn/upload/shuxue/69c3870950b04.pdf；具体作品上传/哈希截止依该官方须知，素材交付不冒称论文提交。用户明确本轮不写论文，因此保留现有Checkpoint，不新作DOCX或全篇W门禁；仅做既有Q4错误解释的最小修正与素材交叉映射。本轮计算素材在P2后冻结，下一轮再进入W1/W2。

作者自检：题面全部4页及6个附件工作簿已读取；Q1–Q4思路与论文入口/构建记录已核；公式、单位、运动学初边值、退化情况、输出域及失败阈值已闭合；无预先编造数值结果。M1需独立审查后才能进入实现。

## 数值验证合同

问题一权威解取 \(\Delta r=0.000125\ \mathrm m\)、\(\Delta t=0.25\ \mathrm s\)。时间误差由保持空间网格不变并将时间步缩小至 0.125 s 检查，空间误差由保持时间步不变并将空间步缩小至 0.0000625 m 检查。前期采用 0.001 m 与 1 s 的网格时，指定位置含水率相对细网格的最大绝对差达到约 0.04 kg/kg，因此不再将题目要求的输出间隔直接作为内部计算网格。问题二前三小时的内部时间步需小于或等于 1 s，并独立检查时间误差。问题三权威步长为 1 s，并用 0.5 s 与 0.25 s 复算终点时长；问题四采用1 s并在最终主网格上以0.5 s复验。

每个模型均需检查矩阵条件数、非线性迭代收敛、温度与含水率范围、径向单调性、表面对流方向、整体水分平衡及网格时间步收敛。所有论文数值必须由唯一复现命令生成，不允许手工填值。
